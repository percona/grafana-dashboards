name: Dashboard Checks

on:
  pull_request:

jobs:
  dashboard-cleanup:
    name: Dashboard cleanup check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get changed dashboard files
        id: changed
        run: |
          git diff --name-only ${{ github.event.pull_request.base.sha }} HEAD \
            | grep -E '^dashboards/.*\.json$' \
            | sort -u > changed_dashboards.txt || true
          echo "count=$(wc -l < changed_dashboards.txt | tr -d ' ')" >> $GITHUB_OUTPUT
          echo "changed_dashboards.txt: $(cat changed_dashboards.txt)" >> $GITHUB_STEP_SUMMARY

      - name: Run cleanup check per dashboard
        if: steps.changed.outputs.count != '0'
        continue-on-error: true
        run: |
          # Ensure we don't exit immediately on the python check
          echo "## 📊 Dashboard cleanup results" >> $GITHUB_STEP_SUMMARY
          echo "| Dashboard | Status | Action |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|--------|--------|" >> $GITHUB_STEP_SUMMARY

          has_failed=0
          # The [ -n "$f" ] ensures the last line is read even if it lacks a newline
          while IFS= read -r f || [ -n "$f" ]; do
            [ -z "$f" ] && continue
            
            if python3 misc/cleanup-dash.py --check-only "$f"; then
              echo "| \`$f\` | ✅ OK | - |" >> $GITHUB_STEP_SUMMARY
            else
              echo "| \`$f\` | ❌ Requires cleanup | \`python3 misc/cleanup-dash.py $f\` |" >> $GITHUB_STEP_SUMMARY
              echo "::error file=$f,title=Cleanup required::Dashboard needs cleanup. Run: python3 misc/cleanup-dash.py $f"
              has_failed=1
            fi
          done < changed_dashboards.txt

          if [ "$has_failed" -ne 0 ]; then
            echo -e "\n> [!CAUTION]\n> Some dashboards failed the cleanup check. See the table above for fix commands." >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
