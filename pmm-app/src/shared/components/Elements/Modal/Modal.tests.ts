import { getInstanceData } from './AddRemoteInstance';

jest.mock('shared/components/helpers/notification-manager');

describe('Modal window::', () => {
  it('Should render header successfully', () => {
  });

  it('Should render body successfully', () => {
  });

  it('Should call onClose callback on close button click', () => {
  });

  it('Should call onClose callback on escape when closeOnEscape is set', () => {
  });

  it('Should NOT call onClose callback on escape when closeOnEscape is NOT set', () => {
  });

  it('Should call onClose callback on background click when closeOnClickaway is set', () => {
  });

  it('Should NOT call onClose callback on background click when closeOnClickaway is NOT set', () => {
  });
});
